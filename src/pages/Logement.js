import React from 'react';
import Header from '../components/Header';

const Logement = () => {
    return (
        <div>
            <Header />
            <h1>Logements</h1>
        </div>
    );
};

export default Logement;